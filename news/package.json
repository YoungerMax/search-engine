{
  "name": "news",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "api": "bun run --filter @news/api dev",
    "fetcher": "bun run --filter @news/fetcher dev",
    "dev:db": "./scripts/dev-db.sh",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "deploy": "./scripts/deploy.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/he": "^1.2.3",
    "drizzle-kit": "^0.31.9",
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "he": "^1.2.0",
    "postgres": "^3.4.8"
  }
}
